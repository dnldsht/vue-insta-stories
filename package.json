{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace example dev",
    "lib:watch": "yarn workspace vue-insta-stories run build:watch"
  }
}